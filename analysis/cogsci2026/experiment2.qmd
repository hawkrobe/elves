---
title: "Experiment 2: Benefit Pressure"
author: "Elves Project Team"
format:
  html:
    toc: true
    toc-depth: 3
    code-fold: true
    fig-width: 8
    fig-height: 5
execute:
  warning: false
  message: false
---

```{r setup}
library(tidyverse)
library(tidyboot)
library(lme4)
library(lmerTest)
library(here)
library(patchwork)

# Color palette
colors_payoff <- c("low" = "#E94F37", "high" = "#2E86AB")

# Output paths
fig_path <- here("latex/figures")
```

# Data Preprocessing

```{r load-data}
# TODO: Update path once data collected
raw_data_path <- here("data/exp2/raw_data")
# raw_files <- list.files(raw_data_path, full.names = TRUE) |>
#   str_subset("inprog", negate = TRUE)
#
# cols_needed <- c("subject_id", "frequency_ratio", "payoff_condition", "survey_difficulty",
#                  "is_correct", "response", "response_text", "target", "angle", "targetFreq",
#                  "rt", "nearest_trained_angle", "timed_out")
#
# raw_data <- map_dfr(raw_files, \(f) {
#   read_csv(f, show_col_types = FALSE) |>
#     select(any_of(cols_needed)) |>
#     mutate(across(everything(), as.character))
# })
```

```{r extract-data}
# TODO: Extract participant conditions, angle mappings, production trials
```

```{r join-frequencies}
# TODO: Join frequency labels and compute dist_to_lf
```

# Sample Descriptives

```{r sample-descriptives}
# TODO: Participant counts by condition
```

```{r timeout-rates}
# TODO: Timeout rates by condition
```

# Critical Trial Selection

```{r filter-critical}
# TODO: Filter to between-frequency trials, create contrast codes
```

# Statistical Models

```{r models}
# TODO: Fit models
# m_payoff <- glmer(
#   responded_lf ~ payoff_c * dist_to_lf + (1 | subject_id),
#   data = d_critical, family = binomial
# )
```

```{r model-summaries}
# summary(m_payoff)
```

# Figure: Payoff Effect

```{r fig-setup}
n_bins <- 9

theme_fig <- theme_minimal(base_size = 12) +
  theme(
    legend.position = c(0.75, 0.85),
    legend.background = element_rect(fill = "white", color = NA),
    panel.grid.minor = element_blank(),
    plot.title = element_text(face = "bold")
  )
```

```{r fig-payoff, fig.width=5, fig.height=4}
# TODO: Create figure with tidyboot CIs and model predictions
```

# Response Time Analysis

```{r rt-summary}
# TODO: RT by condition
```

```{r rt-model}
# TODO: RT model
```

# Summary

```{r summary}
cat("=== EXPERIMENT 2 SUMMARY ===\n\n")
cat("Awaiting data collection...\n")
```
